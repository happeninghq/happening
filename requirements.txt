# Django requirements
django==1.8.2 # Django
pyyaml==3.11 # Used for fixtures
pillow==2.6.1 # For image uploading
django-gravatar2==1.1.4 # For gravatar
django-cleanup==0.3.0 # Remove images when replacing them
django-templated-email==0.4.9 # For sending emails
git+https://github.com/boecklic/django-periodically.git # Periodic tasks
django-ganalytics==0.3 # Include analytics from settings
django-widget-tweaks==1.3 # For editing form attributes in templates
django-markdown-deux==1.0.5 # For rendering markdown
jsonfield==1.0.0 # For JSON serialization
raven==5.1.1 # for Sentry error reporting
django-sslify==0.2.5 # Force SSL
django-pgjson==0.2.3 # Using Postgres JSON field
python-memcached # Memcached bindings
git+https://github.com/rhec/django-html5.git
django-ical==1.3 # For ical feed generation

# Heroku
gunicorn==19.1.1 # For hosting on heroku
dj-database-url==0.3.0 # Heroku databases
dj-static==0.0.6 # Static files
psycopg2==2.5.4 # Postgres driver
django-storages==1.1.8 # Media storage on AWS
boto==2.34.0 # Media storage on AWS

# Static files

django_compressor==1.5 # This allows us to use babel and SCSS
django-libsass==0.3 # Plugin for compressor to use SCSS

# Authentication

django-allauth==0.23.0 # Authentication and social auth

# General

pytz # To stop python moaning about naive timezones
cached-property==0.1.5 # Add cachable properties
vcrpy==1.1.3 # For mocking HTTP requests
python-dateutil==2.3 # For date manipulations
stripe==1.20.2 # For accepting payment
beautifulsoup4==4.3.2 # For parsing HTML
python-dotenv==0.1.3 # To use .env files
